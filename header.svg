<svg width="1200" height="320" viewBox="0 0 1200 320" fill="none" xmlns="http://www.w3.org/2000/svg">
      font-family: 'JetBrains Mono', monospace;
      font-size: 26px;
      font-weight: 500;
      letter-spacing: 4px;
      fill: #C9D1D9;
      opacity: 0;
      animation: fadeIn 2s ease forwards;
      animation-delay: 1s;
    }

    .line {
      stroke: url(#textGradient);
      stroke-width: 3;
      stroke-linecap: round;
      stroke-dasharray: 600;
      stroke-dashoffset: 600;
      animation: drawLine 3s ease forwards;
    }

    .circle {
      fill: #00F5FF;
      opacity: 0.15;
      animation: float 5s ease-in-out infinite;
    }

    .circle2 {
      animation-delay: 1.5s;
    }

    .circle3 {
      animation-delay: 3s;
    }

    @keyframes fadeUp {
      to {
        opacity: 1;
        transform: translateY(0px);
      }
    }

    @keyframes fadeIn {
      to {
        opacity: 1;
      }
    }

    @keyframes drawLine {
      to {
        stroke-dashoffset: 0;
      }
    }

    @keyframes float {
      0%,100% {
        transform: translateY(0px);
      }

      50% {
        transform: translateY(-15px);
      }
    }
  </style>

  <rect class="bg" width="1200" height="320" rx="20"/>

  <circle class="circle" cx="180" cy="80" r="18"/>
  <circle class="circle circle2" cx="1050" cy="120" r="12"/>
  <circle class="circle circle3" cx="920" cy="250" r="20"/>

  <line class="line" x1="260" y1="180" x2="940" y2="180"/>

  <text x="600" y="140" text-anchor="middle" class="mainText">
    DEBASHISH
  </text>

  <text x="600" y="230" text-anchor="middle" class="subText">
    AI ENGINEER | DEVELOPER
  </text>

</svg>
